# .gitignore

# 1. Local .terraform directories (Local Terraform working directories — do not commit/push)
**/.terraform/*

# 2. Terraform State Files (Terraform state files — managed by Spacelift; do not commit)
*.tfstate
*.tfstate.*

# 3. Crash logs
crash.log
*.log

# 4. Sensitive Variables Files (Sensitive variable files — do not upload secrets or keys to GitHub)
*.tfvars
*.tfvars.json

# 5. SSH Keys (SSH private/public keys — DO NOT commit or push)
*.pem
id_rsa
id_rsa.pub
ansible_key
ansible_key.pub
my_key
my_key.pub
ansible_key_pem
ansible_key_pem.pub


# 6. Ansible artifacts
*.retry

# 7. OS generated files (OS-generated files from macOS/Windows)
.DS_Store
Thumbs.db